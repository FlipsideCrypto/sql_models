version: 2
models:
  - name: solana__fact_gov_actions
    description: deprecating soon - please migrate query to solana database by changing 'from' statements from `flipside_prod_db.solana.<table>` to `solana.core.<table>`
    columns:
      - name: PROGRAM_NAME
        description: "name of solana program"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('solana_block_timestamp') }}"
      - name: BLOCK_ID
        description: "{{ doc('solana_block_id') }}"
      - name: TX_ID
        description: "{{ doc('solana_tx_id') }}"
      - name: SUCCEEDED
        description: "{{ doc('solana_tx_succeeded') }}"
      - name: SIGNER
        description: Address initiating the LOCK or EXIT action
      - name: LOCKER_ACCOUNT
        description: Account holding tokens for LOCK or EXIT
      - name: MINT
        description: Mint being locked
      - name: ACTION
        description: LOCK or EXIT action
      - name: AMOUNT
        description: Amount being locked or exited